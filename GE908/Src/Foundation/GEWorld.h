#pragma once
#include "GEModel.h"
#include "GEArray.h"

class GEWorld {
public:
    // Static state

    // Dynamic state

    // Save / Load Logic
    bool saveToFile(const char* path) { return false;  }
    bool loadFromFile(const char* path) { return false; }
};